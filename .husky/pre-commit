#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Load nvm if available (needed when git runs outside interactive shell)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

npx lint-staged
